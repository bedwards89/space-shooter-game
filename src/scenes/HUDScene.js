import Phaser from 'phaser';

// Runs in parallel with GameScene. Displays score, lives, combo, power-up timers.
export default class HUDScene extends Phaser.Scene {
  constructor() {
    super('HUD');
  }

  create() {}

  update() {}
}
