import Phaser from 'phaser';

export default class ShipSelectScene extends Phaser.Scene {
  constructor() {
    super('ShipSelect');
  }

  create() {
    // Phase 2: show owned ships with stats; locked ships show unlock criteria.
  }
}
